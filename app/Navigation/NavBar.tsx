import "./NavStyles.css";

const NavBar = () => {
    return (
        <nav className="h-full flex items-center justify-center">
            <p className="text-lg">Institute of Risk and Safety Analysis</p>
        </nav>
    );
};

export default NavBar;

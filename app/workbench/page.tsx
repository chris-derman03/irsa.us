import React from "react";
import "./testStyles.css";

const WorkbenchPage = () => {
    return (
        <div className="h-screen flex items-center justify-center">Test</div>
    );
};

export default WorkbenchPage;
